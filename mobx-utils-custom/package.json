{
  "name": "mobx-utils-custom",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "dist/mobx-utils.js",
  "types": "dist/mobx-utils.d.ts",
  "scripts": {
    "dev": "vite",
    "build": "tsup src/mobx-utils.ts --dts --format esm --external mobx",
    "preview": "vite preview",
    "test": "vitest"
  },
  "devDependencies": {
    "tsup": "^6.5.0",
    "typescript": "^4.9.3",
    "vite": "^4.0.0",
    "vitest": "^0.25.8"
  },
  "dependencies": {
    "mobx": "^6.7.0"
  }
}
